package ${classpath};

import lombok.Data;

/**
* @author pixel
*/
@Data
public class ${classname} {

<#--<#list attrs as attr>-->
<#--    /** ${attr.remarks} **/ -->
<#--    private ${attr.type} ${attr.name}; -->
<#--</#list>-->

}